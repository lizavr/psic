import { Component } from '@angular/core';
// import {
//   EMAIL_CONTACTS,
//   EMAIL_ICON,
//   TEAMS_CONTACT,
//   TEAMS_ICON,
// } from '@capital/constants';

@Component({
  selector: 'app-support-popup',
  standalone: true,
  imports: [],
  templateUrl: './support-popup.component.html',
  styleUrl: './support-popup.component.scss',
})
export class SupportPopupComponent {
  // protected readonly teamsIcon = TEAMS_ICON;
  // protected readonly emailIcon = EMAIL_ICON;
  // protected readonly teamsContact = TEAMS_CONTACT;
  // protected readonly emailContacts = EMAIL_CONTACTS;
}
